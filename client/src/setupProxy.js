const proxy = require('http-proxy-middleware');

module.exports = function (app) {
    // app.use(proxy('/algorithm', {ignorePath: true, target: 'http://localhost:3000/sample/algorithm.json'}));
    // app.use(proxy('/question', {ignorePath: true, target: 'http://localhost:3000/sample/question.json'}));
    // app.use(proxy('/recommendation', {ignorePath: true, target: 'http://localhost:3000/sample/recommendation.json'}));
    // app.use(proxy('/state', {ignorePath: true, target: 'http://localhost:3000/sample/state.json'}));
};